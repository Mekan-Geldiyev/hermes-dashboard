# Optional — if set, Claude gives REAL reasoning instead of simulated
ANTHROPIC_API_KEY=sk-ant-...

# Optional — URL of the running Python bot (for Amplify / remote deployments)
# Leave blank when running both locally (auto-discovers localhost:8080)
HERMES_BOT_URL=
